{
  "name": "rag-customer-service-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "test": "node --test",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
